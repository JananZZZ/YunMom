name: cloud_mom
description: "云妈妈：Local-first 孕期照料应用"
publish_to: none
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  yunmom_contracts: ^0.1.0
  yunmom_design_system: ^0.1.0
  yunmom_domain: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
