org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# The repository name is intentionally Chinese. AGP's legacy Windows path
# preflight is overly conservative; actual Unicode-path support is verified by
# the build/test pipeline instead of relocating the canonical workspace.
android.overridePathCheck=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
