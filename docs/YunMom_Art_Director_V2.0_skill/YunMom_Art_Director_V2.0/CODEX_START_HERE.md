# Codex start here

1. Confirm the package was merged into the actual project root and `$yunmom-art-director` is discoverable.
2. Let root `AGENTS.md` trigger the Skill for every visual task.
3. For modification work, read the canonical engineering contracts and current visual state, then continue the next unresolved Gate. For review-only work, do not write.
4. Use only task-relevant Skill references; do not restart product ideation or reopen valid Goldens.
5. Generate internally, hard-gate, review independently, integrate, verify, and show at most three finalists. Treat `implementation_ready: false` Tokens as governed scaffolding, not production values.
6. Record explicit feedback and promote only an aesthetic Golden. Keep implementation, matrix acceptance, and release signing separate.
7. Report the exact evidence tier and preserve `RELEASE_NOT_APPROVED` until the final same-build sign-off exists.
